

```
加入一个系统有多个子模块，每个模块由不同的队员开发完成。
1）假设AB模块之间有相互的调用关系。
2）A模块负责学生数据的获取，B模块负责对学生数据进行多种操作，比如对学生数组进行排序。
3）A模块调用B模块提供的排序等功能。
要怎么开始AB模块的同步开发？ 怎么预定方法的签名等。

4）开发B模块的同学在实现的过程当中发现了一个可靠的第三方实现（通常JAR包的形式提供），但是方法的签名与前面约定的不同。
5）假设A模块已经开发完成，不能再做改动。

此时B模块要怎么才可以合理使用第三方的实现来加快代码的实现。 
```
